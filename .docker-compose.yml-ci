version: "3.8"
services:
  nginx:
    image: garutilorenzo/docker-swarm-ingress:localbuild
    build:
     context: .
    ports:
      - 80:80
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
